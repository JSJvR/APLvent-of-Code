 NextEcho←{
     digits←⌈10⍟⍵+1

     ⍝ Odd number of digits: Generate a number of the form 11, 1010, 100100, etc
     2|digits:(10*digits)+10*⌊digits÷2

     ⍝ Split the input into left and right halves and take the larger of the two
     mult←10*digits÷2
     n←(⌊⍵÷mult)⌈(mult|⍵)

     ⍝ Echo the larger half and return it, i.e. if the input has the digits abcdef,
     ⍝ return the larger of abcabc and defdef
     n+n×mult
 }
