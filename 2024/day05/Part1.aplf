 Part1←{
     r u←(0∘≠∘≢¨⊆⊢)⍵
     rules←↑⍎¨¨'|'(≠⊆⊢)¨r
     updates←⍎¨¨','(≠⊆⊢)¨u
     grouped←rules[;2]{⍺ ⍵}⌸rules[;1]

     ⍝ Mask of valid prints
     valid←(grouped∘CanPrint)¨updates

     ⍝ Add up the middle page of all valid prints
     +/{⍵⌷⍨⌈2÷⍨≢⍵}¨valid/updates
 }
