 Reorder←{
     ⍝ Filter to only relevant rules
     rules←⍉(∊⍴¨⍵∘∩¨⍺[;1])/⍉⍺

     ⍝ Filter righthand side, tally and sort
     sorted←rules[;1]⌷⍨⊂⍋≢¨⍵∘∩¨rules[;2]

     ⍝ We might have lost the first page, but we don't
 }
