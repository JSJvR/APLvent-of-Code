 Calc←{
     ⍝ We get all the combinations of +'s and ×'s,
     ⍝ then we append a +, interleave the results with
     ⍝ the numbers and reverse the order. This gives us
     ⍝ a no-op leading +, and it translates from the
     ⍝ puzzle's right-to-left wevaluation to APL's
     ⍝ left-to-right
     ∨/⍺=⍎¨∊¨⌽¨(⍵∘(,,⍤0))¨↓('+',⍨Operators ¯1+⍴⍵)

 }
