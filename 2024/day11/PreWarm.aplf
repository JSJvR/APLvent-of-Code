 PreWarm←{
     ⎕IO←0

     ⍝ Depth
     ⍺←≢cache

     ⍝ Seed values. We want to only populate the entries
     ⍝ we know we hit while processing our input. This will
     ⍝ give us a *very* sparse cache. Blindly populating the
     ⍝ cache beyond a certain depth is very wasteful.

     seed←⊂⍤⍋⍛⌷⍵ ⍝ In theory this will be a bit faster, but I haven't verified it
     ⍝ indices←(⍳⍴cache)[⍳⍺;seed/⍨seed<1⌷⍴cache]
     indices←(⍳⍺)∘.,⍵
     ≢CountNums/¨⊃,/↓indices
 }
